---
apiVersion: api.cerbos.dev/v1
resourcePolicy:
    version: "default"
    # importDerivedRoles:
    #     - common_roles
    resource: "performance"
    rules:
        - actions: ["*"]
          effect: EFFECT_ALLOW
          roles:
              - "admin"
