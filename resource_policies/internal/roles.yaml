---
apiVersion: api.cerbos.dev/v1
resourcePolicy:
    version: "default"
    resource: "internal:roles"
    rules:
        - actions: ["*"]
          effect: EFFECT_ALLOW
          roles:
              - "admin"
