---
apiVersion: api.cerbos.dev/v1
resourcePolicy:
    version: "default"
    resource: "internal:performance"
    rules:
        - actions: ["*"]
          effect: EFFECT_ALLOW
          roles:
              - "admin"
